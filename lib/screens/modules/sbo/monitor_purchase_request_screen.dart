import 'package:flutter/material.dart';

class MonitorPurchaseRequestScreen extends StatelessWidget {
  static const String routeName = 'sbo-monitor-purchaserequest';

  const MonitorPurchaseRequestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Monitor PurchaseRequest'),
      ),
    );
  }
}
